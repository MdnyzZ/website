import { defineConfig } from 'dumi';

export default defineConfig({
  title: 'MdnyzZ',
  favicon: '/image/avatar.jpg',
  logo: '/image/avatar.jpg',
  outputPath: 'docs-dist',
  mode: 'site',
  // more config: https://d.umijs.org/config
});
